I am trying to create a system that obtains data from IoT devices. 
the IoT devices measure water quality data, forwards them to a server and are displayed in a dashbooard in a form a table and graphs.
the system should be able to send notifications and also trigger alerts when setting parameters aexceed acceptable thresh holds.
the system contains 3 major files.
the app.js contains the script for the main app as with the index.html.
the waterqualitydater.js fill contains the script that generates the mockdata for the system when run.
